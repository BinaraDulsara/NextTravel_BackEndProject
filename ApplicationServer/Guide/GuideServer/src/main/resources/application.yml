eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost

server:
  port: 9081
  servlet:
    context-path: /nextTravelPVTLtdGuideService

spring:
  data:
    mongodb:
      database: nextTravelPVTTravelGuideService
      host: localhost
      port: 27017

  application:
    name: GUIDE_SERVER
